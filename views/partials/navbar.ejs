<!-- Navbar -->
<nav class="main-header navbar navbar-expand navbar-white navbar-light">
  <!-- Left navbar links -->
  <ul class="navbar-nav">
    <li class="nav-item">
      <a class="nav-link" data-widget="pushmenu" href="#" role="button" aria-label="Toggle navigation">
        <i class="fas fa-bars"></i>
      </a>
    </li>
    <li class="nav-item d-none d-sm-inline-block">
      <a href="/company" class="nav-link <%= (typeof activePage !== 'undefined' && activePage === 'company') ? 'active' : '' %>">
        Company
      </a>
    </li>
    <li class="nav-item d-none d-sm-inline-block">
      <a href="/clientinfo" class="nav-link <%= (typeof activePage !== 'undefined' && activePage === 'clientinfo') ? 'active' : '' %>">
        Client Info
      </a>
    </li>
    <li class="nav-item d-none d-sm-inline-block">
      <a href="/order-code" class="nav-link <%= (typeof activePage !== 'undefined' && activePage === 'order-code') ? 'active' : '' %>">
        Order Codes
      </a>
    </li>
    <li class="nav-item d-none d-sm-inline-block">
      <a href="/component" class="nav-link <%= (typeof activePage !== 'undefined' && activePage === 'component') ? 'active' : '' %>">
        Components
      </a>
    </li>
    <li class="nav-item d-none d-sm-inline-block">
      <a href="/product" class="nav-link <%= (typeof activePage !== 'undefined' && activePage === 'product') ? 'active' : '' %>">
        Products
      </a>
    </li>
    <!-- System Dropdown Menu -->
    <li class="nav-item dropdown d-none d-sm-inline-block <%= (typeof activeParent !== 'undefined' && activeParent === 'system') ? 'active' : '' %>">
      <a id="systemDropdown" href="#" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" class="nav-link dropdown-toggle">
        <i class="fas fa-cogs mr-1"></i> System
      </a>
      <ul aria-labelledby="systemDropdown" class="dropdown-menu border-0 shadow">
        <li>
          <a href="/factory" class="dropdown-item <%= (typeof activePage !== 'undefined' && activePage === 'factory') ? 'active' : '' %>">
            <i class="fas fa-industry mr-2 text-muted"></i> Factory
          </a>
        </li>

        <li>
          <a href="/transport" class="dropdown-item <%= (typeof activePage !== 'undefined' && activePage === 'transport') ? 'active' : '' %>">
            <i class="fas fa-truck mr-2 text-muted"></i> Transport
          </a>
        </li>
      </ul>
    </li>






  </ul>

  <!-- Right navbar links -->
  <ul class="navbar-nav ml-auto">
    <!-- User Dropdown Menu -->
    <li class="nav-item dropdown user-menu">
      <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown">
        <i class="fas fa-user-circle mr-1"></i>
        <span class="d-none d-md-inline">
          <%= (typeof user !== 'undefined' && user && user.username) ? user.username : 'POC-Admin' %>
        </span>
      </a>
      <ul class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
        <!-- User Header -->
        <li class="user-header bg-primary">
          <i class="fas fa-user-circle fa-4x text-light mb-2"></i>
          <p>
            <%= (typeof user !== 'undefined' && user && user.username) ? user.username : 'POC-Admin' %>
            <small><%= (typeof user !== 'undefined' && user && user.role) ? user.role : 'System Administrator' %></small>
          </p>
        </li>
        <!-- User Menu Footer -->
        <li class="user-footer d-flex justify-content-between">
          <a href="#" class="btn btn-default btn-flat">Profile</a>
          <button type="button" onclick="handleLogout()" class="btn btn-default btn-flat text-danger">
            <i class="fas fa-sign-out-alt mr-1"></i> Sign out
          </button>
        </li>
      </ul>
    </li>

    <!-- Fullscreen Toggle Widget -->
    <li class="nav-item">
      <a class="nav-link" data-widget="fullscreen" href="#" role="button">
        <i class="fas fa-expand-arrows-alt"></i>
      </a>
    </li>
  </ul>
</nav>
<!-- /.navbar -->

<script>
  function handleLogout() {
    if (confirm('Are you sure you want to log out?')) {
      if (typeof $ !== 'undefined') {
        $.ajax({
          url: '/api/auth/logout',
          type: 'POST',
          success: function(res) {
            window.location.href = res.redirectUrl || '/login';
          },
          error: function() {
            // Fallback for POC mode where auth endpoints are disabled
            window.location.href = '/login';
          }
        });
      } else {
        window.location.href = '/login';
      }
    }
  }
</script>
